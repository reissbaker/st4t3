{
  "name": "st4t3",
  "version": "0.0.12",
  "description": "An small-but-powerful typesafe state machine library",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "files": "build/**/*",
  "scripts": {
    "test": "vitest",
    "coverage": "rm -rf ./coverage && vitest run --coverage",
    "build": "rm -rf ./build && tsc"
  },
  "author": "Matt Baker",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-c8": "^0.22.1",
    "vitest": "^0.22.1"
  }
}
